import React from 'react';
import Nav from '../Components/Nav';

const PagesToRead = () => {
    return (
        <div>
        
            <h1>Pages needs to read</h1>
        </div>
    );
};

export default PagesToRead;